<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<!-- Maven build job for building a P2 repository from Q-Gears code review 
		tool and related utilities The generated repository depends on Eclipse indigo 
		repositories, and they are also required during 
		build (see repository URL-s).
		 -->
	<modelVersion>4.0.0</modelVersion>
    <groupId>hu.qgears.review.group</groupId>
    <artifactId>hu.qgears.review.build</artifactId>
	<version>1.0.0-SNAPSHOT</version>
	<packaging>pom</packaging>

	<properties>
		<osgi.os>linux</osgi.os>
		<osgi.ws>gtk</osgi.ws>
		<osgi.arch>x86_64</osgi.arch>
		<tycho-version>0.20.0</tycho-version>
	</properties>
    <repositories>
		<repository>
		<id>indigo-mirror</id>
		<layout>p2</layout>
		<url>${eclipseRepoUrl}</url>
		</repository>
		<repository>
		<id>subversive-site</id>
		<layout>p2</layout>
		<url>${subversiveUpdateSite}</url>
		</repository>
	</repositories>
	<modules>
		<module>../hu.qgears.sonar.client</module>
		<module>../hu.qgears.review</module>
		<module>../hu.qgears.review.eclipse.ui</module>
		<module>../hu.qgears.review.feature</module>
		<module>../../jopt-simple/pom_review_tool.xml</module>		
		<module>../../opensource-utils/commons/hu.qgears.commons/pom_review_tool.xml</module>
	 	<module>p2</module>
	</modules>
	
	<build>
		<plugins>
			<plugin>
				<groupId>org.eclipse.tycho</groupId>
				<artifactId>target-platform-configuration</artifactId>
				<version>${tycho-version}</version>
				<configuration>
					<environments>
						<environment>
							<os>linux</os>
							<ws>gtk</ws>
							<arch>x86_64</arch>
						</environment>
					</environments>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.eclipse.tycho</groupId>
				<artifactId>tycho-maven-plugin</artifactId>
				<version>${tycho-version}</version>
				<extensions>true</extensions>
			</plugin>
			<!--enabling source bundle generation-->
			<plugin>
				<groupId>org.eclipse.tycho</groupId>
				<artifactId>tycho-source-plugin</artifactId>
				<version>${tycho-version}</version>
				<executions>
					<execution>
						<id>plugin-source</id>
						<goals>
							<goal>plugin-source</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
   </build>
</project>
